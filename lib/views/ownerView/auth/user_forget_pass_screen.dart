import 'package:flutter/material.dart';

class UserForgetPassScreen extends StatelessWidget {
  const UserForgetPassScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [],
      ),
    );
  }
}
