import 'package:flutter/material.dart';

class RenterForgetPassScreen extends StatelessWidget {
  const RenterForgetPassScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Column(children: [],),);
  }
}